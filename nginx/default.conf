server {
  listen 80;
  listen [::]:80;
  server_name localhost;

  server_tokens off;

  # Security headers
  include /etc/nginx/conf.d/security_headers.conf;

  # Compression
  gzip on;
  gzip_vary on;
  gzip_proxied any;
  gzip_comp_level 6;
  gzip_types text/plain text/css text/xml application/json application/javascript application/rss+xml application/atom+xml image/svg+xml application/manifest+json;

  brotli on;
  brotli_comp_level 6;
  brotli_types text/plain text/css text/xml application/json application/javascript application/rss+xml application/atom+xml image/svg+xml application/manifest+json;

  root /usr/share/nginx/html;
  index index.html;

  include /etc/nginx/mime.types;
  default_type application/octet-stream;

  types { application/manifest+json webmanifest; }

  # "/" też jest HTML (index.html) -> krótki cache
  location = / {
    include /etc/nginx/conf.d/security_headers.conf;
    add_header Cache-Control "public, max-age=300";
    expires off;
    try_files /index.html =404;
  }

  # HTML z rozszerzeniem .html -> krótki cache
  location ~* \.(html)$ {
    include /etc/nginx/conf.d/security_headers.conf;
    add_header Cache-Control "public, max-age=300";
    expires off;
    try_files $uri =404;
  }

  # Assets (Tailwind build, lokalne JS): długi cache + immutable
  location /assets/ {
    include /etc/nginx/conf.d/security_headers.conf;
    add_header Cache-Control "public, max-age=2592000, immutable";
    expires off;
    try_files $uri =404;
  }

  location / {
    try_files $uri $uri/ =404;
  }
  # Pozostałe statyczne: długi cache + immutable
  location ~* \.(css|js|map|ico|png|svg|webmanifest)$ {
    include /etc/nginx/conf.d/security_headers.conf;
    add_header Cache-Control "public, max-age=2592000, immutable";
    expires off;
    try_files $uri =404;
  }
}
